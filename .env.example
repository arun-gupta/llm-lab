# Standard environment variables (committed to git)
# These are safe to share and are environment-agnostic

# Application configuration
NEXT_PUBLIC_APP_NAME=API Explorer Playground
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Feature flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_DEBUG_MODE=false

# Note: API keys should go in .env.local (not committed to git)
# See .env.local.example for API key configuration 